---
- name: Install virtualenv
  ansible.builtin.apt:
    name: virtualenv
    state: present
    update_cache: true
  become: true

- name: Tempolary Python environment
  ansible.builtin.pip:
    name:
      - pynetbox
    virtualenv: ~/netbox_tmp/venv
