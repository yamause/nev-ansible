---
# Configurations
- name: Create TF with netbox data
  ansible.builtin.template:
    src: netbox.tf.j2
    dest: ~/netbox.tf
    mode: "0644"
  vars:
    netbox_data: "{{ query('netbox.netbox.nb_lookup', 'virtual-machines') }}"
